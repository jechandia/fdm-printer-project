VITE_API_BASE=http://localhost:4000/

# Optional client-side label for the dev-instance badge. Used as a fallback
# when the backend doesn't supply `instanceLabel` on /auth/login-required.
# Leave unset on prod-style installs.
VITE_SERVER_INSTANCE_LABEL=
